import Link from 'next/link'; import {previewLearners,previewChallenges} from '../data'; import '../../young-analysts.css'
export default function Instructor(){return <main className="ya-shell ya-preview"><header className="ya-nav"><Link href="/young-analysts/preview/cohort">← Cohort</Link><strong>Instructor workspace</strong></header><section className="ya-section"><span className="ya-kicker">INSTRUCTOR VIEW · SYNTHETIC</span><h2>Facilitate from evidence.</h2><div className="ya-grid products">{previewChallenges.map(c=><article key={c.id}><span>{c.status}</span><h3>{c.title}</h3><p>{c.skill}<br/>Due {c.due}</p><small>Assignment preview · read-only acceptance test</small></article>)}</div><div className="ya-panel"><h3>Instructional attention</h3>{previewLearners.filter(x=>x.progress<60).map(x=><p key={x.id}><b>{x.name}</b> · {x.progress}% · {x.status}</p>)}<p><small>Signals are instructional prompts, not public rankings.</small></p></div></section></main>}