'use client'
import Link from 'next/link'
import {useMemo,useState} from 'react'
import '../../05/[lesson]/lesson.css'
type Lesson='1'|'2'|'3';type Check={prompt:string;options:string[];answer:number;feedback:string}
const info={'1':{title:'Environments, Projects and Reproducible Foundations',duration:'55–70 minutes',outcomes:['Organise code, configuration, inputs, tests and generated outputs predictably.','Capture the Python version and exact dependency environment.','Replace undocumented manual steps with explicit commands and parameters.']},'2':{title:'Deterministic Data Pipelines and Validation',duration:'60–75 minutes',outcomes:['Design idempotent ingestion, transformation and export stages.','Enforce data contracts and fail visibly when assumptions break.','Control randomness, ordering, time and configuration.']},'3':{title:'Testing, Provenance and Reproducible Publication',duration:'60–75 minutes',outcomes:['Test analytical functions and end-to-end invariants.','Record code, data and output lineage with checksums and run metadata.','Prepare a package another analyst can independently rerun and review.']}} as const
const checks:Record<Lesson,Check[]>={'1':[{prompt:'What is the strongest way to make a Python environment reproducible?',options:['State that pandas was used','Commit a dependency specification or lock file and record the Python version','Email the working laptop','Install the newest packages each time'],answer:1,feedback:'A restorable environment identifies the interpreter and exact or appropriately constrained dependencies.'},{prompt:'Where should a database password be stored?',options:['Inside the notebook','In a secret manager or environment variable outside version control','In the README','In the output CSV'],answer:1,feedback:'Code should reference securely supplied configuration; credentials must not enter repositories or artefacts.'}],'2':[{prompt:'What does an idempotent pipeline step do?',options:['Produces a different result on every run','Can be rerun with the same inputs and configuration without unintended cumulative effects','Requires manual editing','Suppresses validation failures'],answer:1,feedback:'Idempotence makes recovery and reruns safer because repeated execution does not duplicate or corrupt state.'},{prompt:'An input column changes from integer to free text. What should the pipeline do?',options:['Silently coerce every value','Fail with a clear schema-validation error and preserve diagnostics','Delete the column','Continue and hope later code notices'],answer:1,feedback:'Data contracts should detect breaking changes at the boundary, before they contaminate downstream analysis.'}],'3':[{prompt:'Which test best protects a total that must equal the sum of district subtotals?',options:['A font test','A reconciliation invariant tested on every run','A screenshot only','A comment in the code'],answer:1,feedback:'An executable invariant detects drift automatically and documents the analytical rule.'},{prompt:'What most precisely identifies a reproducible analytical run?',options:['The analyst’s memory','Code commit, input versions or checksums, configuration, environment and run timestamp','A final PDF alone','The file name final_v7_really_final'],answer:1,feedback:'Reproduction requires the exact materials and settings that generated the output.'}]}
const structure=[['src/','Reusable ingestion, validation, transformation and analysis code'],['tests/','Unit, integration and regression checks'],['data/raw/','Immutable source snapshots or controlled references'],['data/processed/','Generated analytical datasets—never hand-edited'],['outputs/','Generated tables, figures and reports'],['config/','Non-secret parameters and environment-specific settings'],['README + lock file','Execution contract and restorable environment']]
export default function ModuleSixLesson({lesson}:{lesson:Lesson}){const[selected,setSelected]=useState<Record<number,number>>({});const score=useMemo(()=>checks[lesson].filter((q,i)=>selected[i]===q.answer).length,[lesson,selected]),current=info[lesson];return <main className="ds5-lesson"><nav aria-label="Breadcrumb"><Link href="/data-science">Data Science</Link> / <Link href="/data-science/foundations/06">Module 06</Link> / Lesson 06.{lesson}</nav><header><div><p>LESSON 06.{lesson} · {current.duration}</p><h1>{current.title}</h1><span>Intermediate pathway · analysis that survives handover and rerun.</span></div><aside><b>MODULE 06</b><span>Lesson {lesson} of 3</span></aside></header><div className="ds5-layout"><article><section className="ds5-card"><h2>Learning outcomes</h2><ul>{current.outcomes.map(x=><li key={x}>{x}</li>)}</ul></section>
{lesson==='1'&&<><section><h2>Reproducibility is an execution contract</h2><p>A professional analysis states what code to run, with which inputs, configuration and environment, to regenerate specified outputs. The repository should distinguish immutable source data from generated data and place reusable logic in importable modules rather than burying it across notebook cells.</p><div className="ds5-grid">{structure.map(([name,purpose])=><div key={name}><h3>{name}</h3><p>{purpose}</p></div>)}</div></section><section><h2>Capture the environment without capturing secrets</h2><p>Record the Python interpreter and dependencies using a committed <code>pyproject.toml</code>, requirements file or lock file. Keep credentials in an approved secret manager or environment variables, provide an example configuration with placeholders and document the restoration command.</p></section><section className="ds5-callout"><h2>The notebook warning</h2><p>A notebook that only works after cells are executed in a remembered order is not reproducible. Restart the kernel and run all cells from a clean state. Hidden state is simply undocumented methodology wearing comfortable clothes.</p></section></>}
{lesson==='2'&&<><section><h2>Build one directional, rerunnable pipelines</h2><p>Separate extract, validate, transform, analyse and publish stages. Each stage should accept explicit inputs, create predictable outputs and avoid silent mutation. Use functions, command-line entry points or an orchestrator so the complete workflow can run without manual copying between files.</p><div className="ds5-flow"><b>Extract</b><span>Version source inputs</span><b>Validate</b><span>Enforce contracts</span><b>Transform</b><span>Create analysis-ready data</span><b>Publish</b><span>Generate verified outputs</span></div></section><section><h2>Fail early and diagnostically</h2><p>Validate schema, types, ranges, keys, uniqueness, missingness, row counts and reconciliation totals at defined boundaries. A failed check should identify the dataset, rule, observed value and run—not quietly convert an institutional data problem into an analytical assumption.</p></section><section className="ds5-callout"><h2>Control hidden variability</h2><p>Set and record random seeds where stochastic methods permit it. Sort explicitly, define time zones, freeze extraction dates, avoid machine-dependent paths and record parameters. Exact bitwise reproduction may be impossible for some parallel algorithms; disclose and test an acceptable tolerance.</p></section></>}
{lesson==='3'&&<><section><h2>Test the analytical claim, not only the syntax</h2><p>Unit tests cover focused functions; integration tests cover components working together; regression tests detect unintended changes in approved outputs. Add domain invariants such as valid percentages, unique identifiers, reconciled totals and monotonic cumulative measures.</p><div className="ds5-grid"><div><h3>Unit tests</h3><p>Known inputs, edge cases and expected outputs for small functions.</p></div><div><h3>Integration tests</h3><p>Data boundaries, database calls and connected pipeline stages.</p></div><div><h3>Regression tests</h3><p>Intentional review of changes to trusted analytical results.</p></div><div><h3>Independent rerun</h3><p>A second analyst follows the documented process in a clean environment.</p></div></div></section><section><h2>Make provenance reviewable</h2><ul><li>Identify the repository and immutable commit.</li><li>Record source versions, extraction dates and cryptographic checksums.</li><li>Capture configuration, environment, seed and execution timestamp.</li><li>Link each published output to its producing code and input lineage.</li><li>Retain logs and test results while excluding secrets and personal data.</li><li>Document deviations, non-determinism and unresolved limitations.</li></ul></section><section className="ds5-callout"><h2>Definition of done</h2><p>A reviewer can start from a clean environment, follow the README, verify inputs, execute one documented command, pass the tests and regenerate materially identical outputs.</p><a className="ds5-download" href="/api/data-science/module-06/reproducibility-audit-template" download>Download reproducibility audit template (.csv)</a></section></>}
<section className="ds5-practice"><h2>Guided knowledge check</h2>{checks[lesson].map((q,i)=><fieldset key={q.prompt}><legend>{i+1}. {q.prompt}</legend>{q.options.map((o,j)=><label key={o}><input type="radio" name={`q-${i}`} checked={selected[i]===j} onChange={()=>setSelected(v=>({...v,[i]:j}))}/><span>{o}</span></label>)}{selected[i]!==undefined&&<p role="status" className={selected[i]===q.answer?'correct':'review'}>{selected[i]===q.answer?'Correct. ':'Review this. '}{q.feedback}</p>}</fieldset>)}<p><b>Current score: {score}/{checks[lesson].length}</b></p></section><section className="ds5-apply"><h2>Apply it</h2><p>{lesson==='1'?'Create a repository skeleton and README for one genuine analysis. Specify the environment restoration and execution commands without exposing secrets.':lesson==='2'?'Turn one manual cleaning sequence into a rerunnable function or script with at least five explicit validation checks.':'Add tests for three analytical invariants, then record the code commit, input checksum, run metadata and independent-rerun result.'}</p></section><footer><Link href={lesson==='1'?'/data-science/foundations/06':`/data-science/foundations/06/${Number(lesson)-1}`}>← {lesson==='1'?'Module overview':'Previous lesson'}</Link><Link className="ds5-next" href={lesson==='3'?'/data-science/foundations/06':`/data-science/foundations/06/${Number(lesson)+1}`}>{lesson==='3'?'Return and record Lesson 06.3':'Continue to next lesson'} →</Link></footer></article><aside className="ds5-rail"><b>Progression standard</b><ol><li>Restore the environment.</li><li>Validate every boundary.</li><li>Test analytical invariants.</li><li>Prove code-to-output lineage.</li></ol><p>Return to the module overview to record the persistent lesson gate.</p></aside></div></main>}
